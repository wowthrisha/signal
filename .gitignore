__pycache__/
*.py[cod]
.env
.env.*
!.env.example
*.egg-info/
dist/
build/
.venv/
venv/
node_modules/
.DS_Store
*.log
data/raw/
data/cache/
# Benchmark and replay outputs are committed: the before/after comparison is
# evidence, and a result nobody can inspect is an assertion.
!results/

# The submission archive is built from HEAD; committing it embeds a stale
# copy of itself in the next one.
signal-*.zip
