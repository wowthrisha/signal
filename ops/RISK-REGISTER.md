# RISK-REGISTER.md

**Legend — P (Probability):** H = High · M = Medium · L = Low  
**Legend — I (Impact):** H = High · M = Medium · L = Low  
**Status:** OPEN · MITIGATED · CLOSED · WATCH

Review this file at the start of each phase. Update Status when evidence is logged in ACTION-LOG.md.

---

| ID | Risk | P | I | Trigger | Response | Status |
|----|------|---|---|---------|----------|--------|
| R-01 | NSE URL changed — bhavcopy endpoint moves again | L | H | 404 on fetch | UDiFF URL pinned in `configs/data_sources.json`; probe retries 10 weekdays; fallback to `data/sample/` | **MITIGATED** |
| R-02 | NSE blocks non-browser clients — HTTP 403 / redirect / CAPTCHA | M | H | fetch returns non-200 or empty body | `User-Agent: Mozilla/5.0` + `Referer` on all requests; best-effort cookie warm-up; every session cached under `data/cache/` so re-runs are offline | **MITIGATED 2026-09-04** |
| R-03 | UDiFF parser reads 0 rows because columns differ from legacy schema | M | H | `len(rows) == 0` after parse | **This risk fired on 2026-09-04** — pinned indices were stale by 3 columns. Parser reads by column NAME (indices are fallback only); row-count guard raises `ParseError`; config corrected | **MITIGATED 2026-09-04** |
| R-04 | Unadjusted corporate action produces −90 % false alarm | H | H | Split/bonus without adjustment factor applied | Corp-action adjustment applied **before** log-return computation in `app/normalize/`; `CORP_ACTION` sets `I=2` and supplies `adj_factor`; actions with no derivable ratio are marked unadjustable and suppress detection rather than getting an inferred factor (ADR-017); ISIN succession resolved across face-value changes (ADR-018). Regression over the whole window: adjusted bars below −50 % went **11 → 0** | **MITIGATED 2026-09-05** |
| R-05 | CUSUM floods alerts on volatile sessions | M | M | `h₂` too low; breadth suppression missing | **Did not fire.** Gate 4 measured 0.0446 alerts/user/day at k=5 against a ≤3.0 budget; D2 contributed 132 signals over 9 held-out sessions across 2,883 symbols. Breadth guard implemented and tested (`test_breadth.py`). Cut rule kept executable (`--d1-only`) and verified at [S1.6] | **MITIGATED 2026-09-05** |
| R-06 | LLM credits exhausted mid-demo | L | M | API quota hit | Templates are the MVP default; LLM is behind `--llm` flag; zero impact if flag is off | **MITIGATED** |
| R-07 | Only the first 1000 submissions evaluated by judge — repo not seen | M | H | Late submission to evaluation queue | Submit v1.0 at Gate 5 (T+35), not T+69; iterate in-place after first submission | **OPEN** |
| R-08 | System fails on judge's machine — environment drift | M | H | Missing dep, wrong Python version, port conflict | Gate 8 = fresh-clone test on clean machine (T+61); `docker compose up` must produce a working UI with zero manual steps. **Port conflict already observed 2026-09-04** — a native postgres owned loopback 5432 and silently answered instead of the container; published port is now `${PG_PORT:-5433}` | **OPEN** |
| R-10 | Alert budget measured on a 9-session held-out window | M | M | A tighter budget, or any claim finer than "does not flood" | **Backfill done 2026-09-05**: `bar` now holds **497 sessions (2024-09-02..2026-09-03)**, 1,093,808 rows, up from 127. The published benchmark still scores **9** held-out sessions — widening the *ingest* did not widen the *scoring window*, which is a separate change to `--held-out` plus a recalibration. Measured effect of the longer warm-up: `u_score` saturation fell from 216/20,161 (1.07%) to 73/21,643 (0.34%); precision and recall moved by <0.004. Both result sets committed under `results/` | **OPEN** |
| R-11 | Sector coverage is partial — 750 of 2,883 instruments | L | L | Attribution quality on unmapped symbols | NSE's total-market constituent list carries ~750 names; the rest have no published sector index. §7 already specifies the degradation ("no sector index → market-only attribution; `C ×= 0.8`") and it is implemented and tested, so an unmapped instrument is a supported state rather than a gap | **MITIGATED 2026-09-05** |
| R-09 | Advice language leaks into templates or UI strings | L | H | `buy`/`sell`/`recommend`/`target price` appears in any rendered string | **Correction 2026-09-05: `tests/test_no_advice_language.py` does not exist.** This row previously claimed a CI test that was never written; §21 of the spec names the file and it was never created. What is actually in place is the persistent footer — *"Signal surfaces information about what changed. It does not provide investment advice."* — and templates confined to `app/templates/headlines.py`. Nothing enforces the constraint automatically | **OPEN** |
| R-12 | `app.benchmark` determinism unproven | M | M | Any published metric changing between two runs with identical arguments | **CLOSED 2026-09-05.** Two consecutive `make evaluate` runs produce byte-identical `metrics.json` with only `generated_at` differing. The two committed runs this row originally cited (B0 alerts 3250 vs 3251) were never evidence of nondeterminism — one passes `--history-from 2026-02-27` and the other does not, so a one-alert difference is the expected consequence of a different warm-up. The real gap was that nothing asserted it; `tests/test_benchmark_determinism.py` now does, including a guard so the volatile-key exclusion list cannot grow silently | **CLOSED 2026-09-05** |
| R-13 | Ground truth is dividend-dominated | H | M | Any claim that these figures measure usefulness to a reader | **Hypothesis TESTED AND REJECTED 2026-09-05.** The claim was that dividends are announcements without price materiality and therefore depress precision. Measured: dividend ex-dates carry mean `abs(z)` of 0.9246 against a matched-baseline 0.7659 — a ratio of **1.21** over 2,696 events, not the ~1.0 predicted. Splits/bonuses/rights/buybacks run 1.80–2.42, so a dividend is a weaker signal but not an inert one. Prediction was committed at `b93299a` before the test ran. Label kept unchanged; no L1/L2 built, because constructing a label after its stated justification fails is the fitted move. The residual limitation stands: the label measures announcement co-occurrence, not reader relevance, and `alert_reduction_vs_B0` is the headline because it needs no label | **OPEN, narrowed** |
| R-14 | AWS deployment blocked by IAM scope | M | L | A requirement that the demo run on AWS specifically | `apprunner:*` and `rds:DescribeDBInstances` return AccessDenied for user `aivar-deploy`; ECS/EB/Lightsail/EC2/ECR permitted. Railway is the official demo; the root `Dockerfile` is not Railway-specific and deploys unchanged once RDS is granted. See ADR-034 | **OPEN** |
| R-15 | Corporate-action feed has no row for 24 price gaps — cliff regression xfails | M | M | An adjusted bar falling below -50 % | Missing source data, not an adjuster defect. `INE012Q01039` drops 82 % on 2026-05-19 while its only feed entries are dated 2026-04-02. 24 bars, 23 of 2,935 symbols, all between 2024-10-25 and 2026-05-19, **none in the benchmark's held-out window**, so no published metric is affected. Test is `xfail(strict=False)` with the evidence in the reason string, so it self-clears when the feed is fixed. Closing needs a second corporate-action source to cross-check the first | **OPEN** |
| R-16 | Session interrupted mid-write by machine sleep | L | M | Uncommitted or half-written files after an interruption | Fired 2026-09-05. Recovery ran `git status`, `git log`, the full suite, and a live curl before touching anything. Result: no partial writes — `index.html` was untouched and the suite had *risen* to 215 passed / 1 xfailed. Uncommitted work was complete and was committed as-is. Standing response: establish state from commands before editing, and commit after each UI step so an interruption costs one step rather than four | **MITIGATED 2026-09-05** |
| R-17 | Shared demo state — one visitor's ack blanked the public demo for everyone | H | H | A public URL with a single user row that visitors can write to | **Fired 2026-09-05.** The live URL served `32 watched / 0 moved / 0 surfaced` because someone pressed "Mark all as seen" and the cursor persisted on `DEMO_USER_ID`. Silent: nothing on the page indicated a fault. Fixed with per-visitor state — the browser mints a uuid into localStorage and sends `X-Signal-Session`; a new session clones the template watchlist with a null cursor. `DEMO_USER_ID` is now read-only to visitors and the seed prunes anything the turnover query did not put on it. Isolation, not auth: the header is self-asserted and grants only a private copy of a public list | **MITIGATED 2026-09-05** |
| R-18 | Symbol resolved to a superseded ISIN — duplicate chip, inflated `watched` | M | M | A symbol mapping to more than one ACTIVE ISIN | **Fired 2026-09-05.** 124 symbols map to 2+ ISINs because a face-value change mints a new one and NSE leaves both ACTIVE. Resolution ordered by `isin`, so MCX resolved to `INE745G01035` (last bar 2026-01-01) instead of `INE745G01043` (first bar 2026-01-02). The dead ISIN can never produce a card, so the chip was permanently empty and `watched` was one too high. Both resolution and rendering now break the tie on latest bar; `DISTINCT ON` folds an already-added superseded ISIN. **Not double-scoring**: all 124 pairs hold non-overlapping contiguous ranges, which is a succession, and a test guards that premise — an overlap would mean two real instruments and deduplicating would then hide one | **MITIGATED 2026-09-05** |
| R-23 | Market-regime suppression has never fired on real data | L | M | A market-wide session reaching `breadth > 0.5` | **Measured 2026-09-05 across all 497 sessions: zero qualify.** Maximum breadth is **0.3473 on 2026-04-01** (838 of 2,413 symbols beyond |z| > 2). The threshold was **not** lowered and no demo control was built, because the only way to render a regime session from this data is to move the gate. The logic is implemented and unit-tested (`tests/test_breadth.py`); what is missing is a real session, and two years without one is arguably evidence the threshold is set sensibly rather than evidence of a defect | **OPEN, unexercised** |
| R-26 | Evidence cannot be ordered against the price move | H | M | Any implication that an attached document existed before the movement | **Partly resolved 2026-09-05.** Ingesting NSE corporate *announcements* — a different endpoint from corporate *actions* — supplied `exchdisstime`, a real dissemination moment (869 distinct HH:MM, zero midnights). Orderable events went **0 -> 1,231 of 5,962 (20.6 %)**; evidence rows 4,072 -> 48,284, of which 43,704 now carry a document URL. **Still open** for the 4,072 ex-date rows, which stay UNKNOWN by rule, and for the ledger's first two years, since announcements were ingested for 2026-06-01 onward only | **OPEN, narrowed** |
| R-36 | Lookahead: a forward outcome reaching selection | L | H | Any engine or selection path reading an outcome | **Guarded mechanically, and the first guard did not work.** Payload equivalence (outcomes on vs off) only catches leakage *gated on the flag*: a deliberately injected leak that ran in both branches corrupted both equally and compared equal, while 6 real `for_event` calls happened during the supposedly outcome-free build. Two guards now: no `app/engine/` module may import the outcomes module (ast walk), and selection must complete with `for_event` monkeypatched to raise. Proved by injecting the leak (guard fails) and reverting (24 pass). Consequence if it ever regresses: every benchmark figure becomes optimistic and no test goes red | **MITIGATED 2026-09-05** |
| R-37 | Card-dependent tests pass vacuously on the fixture database | M | H | Any test asserting over `payload['cards']` | **Found 2026-09-05.** `conftest.SEED_TABLES` copies bars but deliberately not `event`, so the fixture digest surfaces no cards and every leakage assertion iterated an empty list — green while a live injected leak ran through them. The `1 skipped` in earlier runs was the only visible tell. `test_outcome_leakage.py` now seeds the watchlist's events and `_require_cards()` fails loudly rather than passing on nothing. **Other card-dependent tests have not been audited for the same vacuity** | **OPEN, narrowed** |
| R-35 | Concurrent pytest sessions destroy each other's database | M | M | Two test runs overlapping — a backgrounded suite plus a foreground one, or CI parallelism | **Fired 2026-09-05.** `conftest.py` used a fixed `signal_test` name and `DROP DATABASE ... WITH (FORCE)` at setup, so the second session tore down the first mid-run: `337 passed, 47 errors in 13.67s`, green on an immediate re-run — the shape that gets dismissed as flake. Reproduced deliberately by running two modules at once (7 setup errors across the pair), then fixed with a per-process database name; the reproduction now passes both sides. The isolation guard had pinned the literal name alongside the real invariant and was relaxed to a prefix check — the not-the-dev-database property is unchanged | **MITIGATED 2026-09-05** |
| R-33 | Span-replacement deletes a function whose call site survives | H | H | Any edit computing `s[index(A):index(B)]` over source | **Fired 2026-09-05, in production.** The card compaction replaced `cardHTML..filteredHTML`; `chainHTML` sat between them and was deleted while its call site remained, so the live page threw `ReferenceError: chainHTML is not defined` and rendered no cards. Third instance of this shape after `freshnessText` and R-25. Restored verbatim from the parent commit — no `typeof` guard, because hiding it would drop the evidence chain silently. Response: `tests/test_render_execution.py` executes the render path in node and fails on any throw | **MITIGATED 2026-09-05** |
| R-34 | Markup-inspection guards cannot see a runtime failure | H | H | Any UI guard that asserts on source text rather than executing it | **Measured 2026-09-05: with `chainHTML` deleted, 47 markup-inspection tests passed on a page that crashed on load**, while the new execution test failed 3 of 4. 375 tests were green when the ReferenceError shipped. Every guard added before this one — hex literals, ARIA, claim language, hardcoded numbers, README sections — reads source and never runs it. Response: at least one guard must execute the artifact end to end; static checks are a complement, not a substitute | **MITIGATED 2026-09-05** |
| R-31 | A guard can pass while missing the case it exists to catch | H | H | Any guard whose failure mode has never been exercised | **Fired 2026-09-05.** The `/lab` hardcoded-number guard walked numeric AST constants only. Injecting a real displayed metric as text — `f"<p>max breadth 0.3473 across 497 sessions</p>"` — **passed cleanly**, because to `ast` those digits are inside a string, not numbers. The guard was blind to the primary failure it was written for, and had looked green since the day it was added. Fixed by scanning string literals with the CSS template exempted by name, and re-verified with the same injection, which now fails the suite. **Standing response: exercise the failure path, not just the success path** — R-27 required a fires-test and one existed, but it probed the wrong shape | **MITIGATED 2026-09-05** |
| R-32 | Polling implied a liveness the data does not have | M | M | Any refresh cadence faster than the data changes | **Fixed 2026-09-05.** The page polled `/api/digest` every 5 seconds against end-of-day bars, so 719 of every 720 requests could not return anything new — and a fast pulse on a page arguing for *not* reacting to noise contradicts the product where a reader can see it. Now: refetch on mutation and on tab focus, one 60s background check while visible with conditional-request semantics, and a cadence line derived from `max(session_date)` saying "next update after market close" | **MITIGATED 2026-09-05** |
| R-30 | A page that reads artifacts can fail silently in the container | M | M | Any surface reading a repository file at request time | **Fired 2026-09-05.** Signal Lab resolved `results/` and `ops/` by walking up three parents, which holds in a checkout and not in the container, where compose mounts `backend/` at `/app`. The page returned HTTP 200 and rendered "no artifact" text indistinguishable from a legitimate empty state. Fixed both ways: the image COPYs both directories, compose mounts them read-only so a regenerated benchmark needs no rebuild, and `_artifact_root` prefers the checkout so local edits win. **The residual risk is the silence** — an artifact-backed page degrades to plausible-looking emptiness rather than erroring, and only a content assertion catches it; `test_lab_figures_come_from_the_artifacts_not_the_template` is that assertion | **MITIGATED 2026-09-05** |
| R-29 | The "no weights" claim could be quietly scoped away | L | H | Any edit to `tests/test_salience.py` that adds an exclusion, allow-list or carve-out | **Pressure applied 2026-09-05.** A fuzzy challenger needed Mamdani defuzzification, which is a weighted average, and the AST guard fails on arithmetic joining two score names. The guard was **not** touched: the challenger lives in `app/engine/fuzzy/`, outside its scope, and the claim is now stated explicitly as being about the *shipped* policy (ADR-045). `test_fuzzy_policy.py` asserts the module is not under `salience/` and re-runs the guard's own logic, so relocating the challenger into the protected directory breaks the build in a place that explains why. A guard with an exception in it cannot answer "how did you justify your weights?" | **MITIGATED 2026-09-05** |
| R-28 | `FOLLOWS` is unreachable from the only source that can produce it | L | L | Reading the zero `FOLLOWS` count as evidence of clean data | **By construction, not by luck.** `attach_session` sends a post-close announcement to the *next* trading session, so no ingest path can place a document in a session that had already closed. The classifier supports `FOLLOWS` and is unit-tested for it; the live count is 0 and will stay 0 until a source attaches evidence by some other rule. Stated in the README because an unexplained zero reads as a quality signal it is not | **OPEN, disclosed** |
| R-27 | A guard can silently match nothing | M | H | Any regex guard added without a test proving it fires | **Fired 2026-09-05.** The claim-language guard was written through a non-raw heredoc, so `\b` became a literal backspace byte and every pattern matched nothing — the guard passed on every file while enforcing nothing. Caught only by its own meta-test. Response: every guard carries a test asserting it matches a known-bad string and skips a known-good one, as `test_no_advice_language.py` already did | **MITIGATED 2026-09-05** |
| R-25 | Span-replacement edits silently delete unrelated content | M | M | Any edit of the form "replace everything between heading A and heading B" | **Fired 2026-09-05.** Regenerating the README's scaling section in [U6.3] replaced the whole span from `## How this scales` to `## What we deliberately did NOT build`, silently removing `## What is actually new here` — the GR-1 prior-art discussion added in [U4]. No test covered it, because prose has no test; found only because a later edit happened to list the headings. Restored verbatim from `42030ee^`. **Now guarded:** `tests/test_readme_sections.py` asserts 12 required `##` sections and 5 required `###` disclosure subsections, that none is effectively empty, and that no heading is duplicated. The guard was verified by deleting `## Evidence` and confirming the suite fails, then restoring it. Editing practice also changed: replace a block only after asserting its contents, or append before an anchor — never an unread span | **MITIGATED 2026-09-05** |
| R-24 | Evidence coverage is 17.6 % of events | M | M | Any implication that every card has a primary source | **Measured 2026-09-05: 1,052 of 5,962 events.** By type: CORP_ACTION 987/987, JUMP 44/3,806, DRIFT 21/1,169. This is mostly not a gap — Tier C means "unusual movement, no known cause", so a JUMP with no filing is the system working, and the card renders "No corroborating event found" rather than hiding the block. The real limitation is that the corporate-actions feed carries no per-record permalink, so `url` is NULL throughout and no card can link an original | **OPEN, disclosed** |
| R-22 | A summary sentence cites a pre-chosen reason rather than the dominant one | M | M | Any prose that names one bucket from a distribution it did not inspect | **Found 2026-09-05.** The funnel subline always cited `explained_by_market`, which is 4 of 18 on the live digest while `below_threshold` is 14. Every clause was true and the overall impression was wrong — misleading by omission, which is the failure mode this product exists to argue against. The sentence now selects the largest reason from the response at render time. **Not** the false claim the brief predicted: the subline said 18 were *filtered*, never 18 were *explained* | **MITIGATED 2026-09-05** |
| R-21 | Query plan shape changes as tables grow — `watchlist_item` now seq-scans | L | L | A plan regression on the hot path | **Observed 2026-09-05, and it is the correct plan.** `watchlist_item` grew 30 -> 151 rows across 5 users when per-visitor state began cloning the template, and at 2 heap pages / 16 kB the planner reads it whole rather than descending an index. `Rows Removed by Filter: 121`. **No index added** — `watchlist_item_pkey` exists and the planner will switch to it unprompted once that wins. The table that actually grows, `event`, is still reached via `event_isin_id` with a `Memoize` node; `Execution Time: 1.196 ms`. Documented in the README under "How this scales" rather than indexed away | **MITIGATED 2026-09-05** |
| R-20 | Every published rate is measured in the calmest regime in the corpus | M | H | Any reader treating the quoted alert rate as typical | **Measured 2026-09-05.** NIFTY annualised realised volatility over the 9-session hold-out is **6.00 %**, against 11.46 % over 100 sessions and 15.58 % over 150 — the hold-out is 91 % calmer than a 100-session window. Ground-truth density also swings from 6.74 to 16.58 positives/session across candidate widths, so precision and recall are not comparable across windows. Widening was investigated and rejected: no leakage at any width (estimation is `[t-120, t-1]`), but pooling regimes yields one average describing neither, and per-regime reporting did not fit. Disclosed in the README instead — the real-world alert rate is very likely higher than the quoted one | **OPEN, disclosed** |
| R-19 | Competitive claim goes stale — Groww shipped GR-1 in Aug 2026 | M | M | Any README wording resembling "nobody combines AI with market context" | No such claim was present; the README now cites GR-1 as prior art explicitly and narrows the claim to structure — separated detection/attribution/allocation, measured suppression, deterministic replay, provenance on every displayed number — and states that uniqueness is not something this repository can prove. **Unverified by this repo:** the GR-1 launch date is taken from the brief, not independently confirmed | **OPEN** |

---

---

## Mitigation evidence

**R-04 — MITIGATED 2026-09-05.** Eleven raw single-bar moves below −50 % in the
ingested window, every one a split, bonus, rights issue or demerger. After
adjustment: **zero.** The known splits are checked individually
(`test_the_known_splits_are_adjusted_not_merely_suppressed`) so that suppressing
every large move cannot pass as adjusting them. Two things the spec does not
spell out had to be solved: demergers carry no ratio in the feed (ADR-017), and a
face-value split issues a new ISIN so 241 of 1,016 actions arrived keyed to an
identifier with no bars (ADR-018). Evidence: ACTION-LOG [S0.2].

**R-05 — MITIGATED 2026-09-05.** The flood did not happen. `alerts_per_user_day`
= 0.0446 at k=5 on a held-out window against a ≤ 3.0 budget, estimated two ways
that agree to 3 %. D2 stays. Evidence: ACTION-LOG [S1.5].

**R-02 — MITIGATED 2026-09-04.** 135-weekday backfill against
`nsearchives.nseindia.com`: **127 sessions succeeded, 8 upstream 404s (exchange
holidays), 0 failures, 0 blocks.** No 403 on any archive fetch. The cookie warm-up
against `www.nseindia.com` does itself return 403, but the archive host serves the
browser-headered request regardless, so the warm-up is best-effort and non-fatal.
Evidence: ACTION-LOG [F1.4].

**R-03 — MITIGATED 2026-09-04.** The risk was real, not hypothetical: the pinned
column map in `configs/data_sources.json` was three columns stale (`ClsPric` 13→17,
`TtlTradgVol` 21→24), and index 13 pointed at a company-name string. Because the
parser resolves by column name, the live header was read correctly and the stale
pins were never used. Post-ingest assertions confirm it: `bad_close = 0`,
`inverted (h < l) = 0`, `null_close = 0` across 311,769 bars.
Evidence: ACTION-LOG [F1.2], [F1.6].

**R-08 — note added 2026-09-04.** A second environment-drift instance was found
and fixed during F2: `make evaluate` needed `pyyaml`, which was absent from
`backend/requirements.txt`. Added, along with `pytest`. R-08 stays OPEN — the
fresh-clone test at Gate 8 is the only thing that can close it.

## Update protocol

When a risk is mitigated:
1. Add the mitigating command + output to `ops/ACTION-LOG.md`.
2. Change Status to **MITIGATED** and note the date.
3. Never delete rows — the register is an audit trail.

_Last updated: 2026-09-04_

---

# Detail

## R-12 — `app.benchmark` determinism is unproven — OPEN

**Observed:** two runs at git_sha `9d93aaa`, 51 seconds apart, disagree. `results/20260905T034248Z_short_history` reports B0 alerts **3250** and `alert_reduction_vs_B0` **0.956308**; `results/20260905T034339Z_full_history` reports **3251** and **0.948939**. Both are committed and both are real.

**Assessment:** these two runs are *not* a fair determinism test — the first passes `--history-from 2026-02-27` and the second does not, so a one-alert difference is expected, not evidence of nondeterminism. What is genuinely open is that **no test asserts two identical invocations produce identical output**. Gate 2 covers `app.evaluate` (the replay harness), not `app.benchmark`.

**Trigger:** any published metric changing between two runs with identical arguments and an unchanged database.
**Response:** add a determinism test invoking `app.benchmark` twice with identical arguments and diffing `metrics.json` with `generated_at` and `git_sha` excluded.
**Evidence:** the two committed `metrics.json` files named above.
**Status:** OPEN. Not closed by inspection, because the honest statement is "untested", not "correct".

## R-13 — Ground truth is dividend-dominated — OPEN

**Observed:** the benchmark labels a `(isin, session)` positive when a corporate action gives `I >= 2`. Of 4,072 corporate actions in the corpus the overwhelming majority are dividends — reproduce with `SELECT ca_type, count(*) FROM corp_action GROUP BY 1 ORDER BY 2 DESC`.

**Assessment:** a dividend ex-date is an announcement, not necessarily a price-material event. Precision and recall in `results/latest` are therefore measuring **co-occurrence with a scheduled announcement**, not relevance to a reader. `alert_reduction_vs_B0` is unaffected because it requires no label, which is why it is the headline number.
**Trigger:** any claim that these precision/recall figures measure usefulness.
**Response:** implement §14's second label (market-confirmed `|CAR[-1,+1]| > 2σ`) and report both. Not attempted under deadline; changing the label after seeing results would be worse than stating the limitation.
**Evidence:** `results/latest/metrics.json` `ground_truth` block, which carries the limitation inline.
**Status:** OPEN and disclosed in the README.

## R-14 — AWS deployment blocked by IAM scope — OPEN

**Observed:** `aws apprunner list-services` and `aws rds describe-db-instances` both return AccessDenied for `arn:aws:iam::870591755696:user/aivar-deploy`. ECS, Elastic Beanstalk, Lightsail, EC2 and ECR are permitted.
**Trigger:** a requirement that the demo run on AWS specifically.
**Response:** grant the IAM user RDS and App Runner, then deploy the existing root `Dockerfile` unchanged. See [ADR-034](../docs/DECISIONS.md).
**Evidence:** the two AccessDenied responses, quoted in the session log.
**Status:** OPEN. Railway is the official demo; nothing in the image is Railway-specific.
